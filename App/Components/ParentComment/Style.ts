import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#a1a1a1',
    padding: 8,
  },
  containerAuthor: {
    flexDirection: 'row',
  },
  authorText: {
    marginRight: 8,
  },
  closeBtn: {
    position: 'absolute',
    right: 4,
    top: 4,
  },
});
